{
    "builds": [{
        "src": "djangobackend/wsgi.py",
        
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "djangobackend/wsgi.py"
        }
    ]
}